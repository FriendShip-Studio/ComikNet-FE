export interface HistoryRecord {
    cid: string;
    update_date: string;
    update_time: string;
}

export interface Comment {
    aid: string;
    uid: string;
    comment: string;
    comment_time: string;
}